management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include: "*"
  prometheus:
    metrics:
      export:
        enabled: true
  endpoint:
    metrics:
      enabled: true